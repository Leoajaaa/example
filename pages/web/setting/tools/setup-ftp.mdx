import Image from "next/image";
import { Steps, Tabs, Tab } from "nextra/components";

import { Callout } from "nextra/components";
import { Bleed } from 'nextra-theme-docs';

import ftpu1 from "@/public/image/web/setting/tools/ftp/u1.png";

# Setup FTP

## update ftp 

<Steps>

### Info FTP

Isikan pada text box host, port, username, password dan sub.

<Image
            src={ftpu1}
            
            width={1000}
            height={1000}
                
        />
<Callout type="info" emoji="ℹ️">
    `Tanda (*) pada atribut isian, menandakan bahwa isian tersebut wajib diisi/tidak boleh kosong`
</Callout>


### klik **Update**

Klik tombol **Update** untuk menyimpan perubahan setting ftp

</Steps>

## FAQ

export function FAQBox({ title, children }) {
    return (
        <details
            close
            className="last-of-type:mb-0 rounded-lg bg-neutral-50 dark:bg-neutral-800 p-2 mt-4"
        >
            <summary>
                <strong className="text-md">{title}</strong>
            </summary>
            <div className="nx-p-2">{children}</div>
        </details>
    );
}

<FAQBox title="Apakah FTP bisa dihapus ?">
  **Tidak**, Setting FTP tidak dapat dihapus, jika FTP sudah diganti maka dapat dilakukan Update FTP saja. 
  
</FAQBox>